# The tests of this package include some windows binaries. These are necessary
# for the package's tests to pass
git-lfs source: source-contains-prebuilt-windows-binary

# Upstream does not provide gpg signed releases
git-lfs source: debian-watch-may-check-gpg-signature
